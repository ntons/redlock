syntax = "proto3";

package distlock;

option go_package=".;distlock";

// Lock represents an obtained, distributed lock.
// This structure can be marshaled/unmarshaled by protobuf,
// and transfered to anywhere keeping the lock state.
message Lock {
    string key   = 1;
    string value = 2;
}
